export {
  currentMedia,
  handleEnded,
  isVideo,
  markPlaybackError,
  openSession,
  setContinuous,
  setIntent,
  setProgress,
  setStatus,
  stepSession,
} from './session';
export type { PlaybackIntent, PlaybackStatus, Session } from './session';
