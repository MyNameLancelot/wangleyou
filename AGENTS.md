# 开发指导（SDD）

## 1. 范围与阅读顺序

本文件适用于整个仓库。项目采用 SDD（Spec-Driven Development，规格驱动开发）：先明确规格和计划，再实施、验证、同步基线和归档。

每次工作按需阅读：

1. 本文件：长期开发规则和流程。
2. [产品需求](docs/requirements.md)：当前产品基线与验收行为。
3. [总架构](docs/architecture.md)：当前架构约束、模块边界及实施状态。
4. 本次 `docs/changes/<变更名称>/` 的规格、计划和任务。
5. 受影响模块的 `module.md` 及相关架构决策。

用户最新明确指令优先。子目录指导只能补充局部约定；发现文档冲突时定位并同步修正，不静默选择互相矛盾的要求。

文档职责不得混淆：需求描述做什么，总架构描述系统如何组织，变更记录描述本次如何演进，README 描述如何使用，module.md 描述模块契约。避免复制整段规格造成多个事实来源。

## 2. 不可擅自改变的边界

网站是公开访问的纯静态家庭影像网站，媒体与配置在 GitHub 仓库维护，兼容 PC、移动端和未来 WebView 套壳。完整功能及非目标见需求文档。

- 不擅自引入业务后端、数据库、登录、在线内容管理或付费服务。
- 不未经用户明确指示推送代码或公开发布网站。
- 保护已有代码和用户未提交改动，不做无关重构。
- 本期不实现原生桥接；不要为未来可能的能力创建空模块。

## 3. 文档与状态管理

目录约定：

- `docs/requirements.md`：当前产品需求基线。
- `docs/architecture.md`：总架构，明确已实现与目标设计。
- `docs/decisions/`：重要架构决策、备选方案和取舍。
- `docs/changes/<变更名称>/`：活动变更，包含 `spec.md`、`plan.md`、`tasks.md`。
- `docs/archive/<日期>-<变更名称>/`：完成变更及验证记录。

具体字段与归档条件见 [变更规范](docs/changes/README.md) 和 [归档规范](docs/archive/README.md)。

“必须”是执行约束；“建议”允许说明理由后调整；未决事项必须标明决策时点，不能伪装成已实现功能。

## 4. SDD 执行流程

1. **理解现状**：检查代码、文档、活动变更及用户改动，确定范围。
2. **编写规格**：在活动变更目录记录问题、目标、非目标、行为、边界和可编号的验收条件。
3. **确认交互要求**：页面、流程、组件语义、主题或重要响应式变化以用户最新口述为准；将已确认内容写入本次规格和验收项，不维护外部设计稿。
4. **制定计划**：列出影响模块、页面、依赖顺序、实施步骤、验证方式和回退方式，再拆为任务。
5. **实施**：按任务执行并更新状态；常规细节自行决定并记录，不反复请求已有授权。
6. **处理偏差**：实施前更新受影响的规格或计划；若改变已确认范围，先取得用户确认。
7. **验证**：按验收项记录证据，完成相关构建、检查和行为核对；失败或阻塞保持未完成。
8. **同步基线**：更新需求、总架构、module.md、README 和必要决策，标明真实实施状态。
9. **归档**：满足完成条件后移动整个变更目录，更新相对链接和索引；归档与 Git 提交、推送、发布相互独立。

完整 SDD 适用于行为、验收标准、数据结构、模块职责/接口/依赖、关键状态/资源生命周期，以及具有产品或架构影响的构建部署变化。

轻量流程适用于不改变行为的姓名/文案/排版修正、恢复明确既有规格的局部 bug 修复、无产品或架构语义变化的维护调整。轻量只需 change.json 中的理由、文档影响与验证证据，不强制创建三份 Markdown。不得仅凭行数、扩展名或“bug”标签豁免；有行为或架构变化必须 full。

每个 PR 必须新增或更新变更目录中的 change.json，声明覆盖整个 PR（多个记录共同覆盖），逐项判断基线影响。需更新的文档实际更新，不适用项写理由。实施前建立记录；变更范围调整时同步，提交前填写实际验证。规则、字段、命令与审查清单见 [SDD 维护指南](docs/sdd.md)。CI 通过不等于语义正确。

不维护 Penpot 或其他外部设计稿，也不在 `change.json` 使用设计影响分级。用户可见变化仍须根据实际行为选择 full 或 light：新增或改变页面、流程、交互语义、组件语义、主题、重要响应式、空状态或错误恢复时，必须使用 full，并将用户已确认的口述要求写入规格、计划和验收项。

恢复工作时先读活动变更任务及最近验证记录，继续未完成部分，不重建已完成计划。若存在多个活动变更，各自写明影响模块和依赖，不擅自合并范围。

## 5. 模块与编码规则

- 按业务能力独立目录，组件、逻辑、样式、类型与测试就近组织；不强制独立 HTML。
- 遵循总架构依赖方向，通过模块公开入口协作，禁止循环依赖和跨模块内部引用。
- 当前媒体、播放队列和调度状态归 playback；查看器不维护第二套业务状态。
- shared 不承载具体业务流程，不作为杂项代码堆积处。
- 主题 UI（JSX、CSS、装饰与资产）按主题目录完整隔离，主题之间不得导入；只允许共享无 UI 的类型、纯领域逻辑与交互契约。
- 实现具有一定复杂度的通用能力前，依次检查项目现有实现或依赖、标准平台能力及维护活跃的成熟第三方库；存在合适方案时优先复用，避免重复实现，也不得为简单逻辑机械增加依赖。新增依赖或自行实现均须在变更记录中简述候选方案、最终选择和理由，具体评估标准见 [SDD 维护指南](docs/sdd.md#依赖与自行实现评估)。
- 事件、计时器、媒体资源和异步任务必须有明确的创建、失效及清理归属。
- 不把真实相册数据写死在 UI；内容配置与展示逻辑分离。

每个实际模块的 `module.md` 必须包含：目的、职责、非职责、公开接口与输入输出、允许依赖、状态及资源生命周期、主要文件、扩展和验证方法。接口或职责变更必须同步更新。

## 6. 工具与验证

技术栈已确定为 React + TypeScript + Vite，使用 Node 24 和 npm，依据 docs/decisions/0001-frontend-foundation.md。真实命令见 README；常用验证为 npm run check、npm run build、npm run test:e2e。新增或修改工具命令时同步 README，不适用项说明原因。

- 统一包管理器，提交对应锁文件，记录运行时版本，保证安装可复现。
- 使用与变化相称的验证；重点测试内容校验、媒体边界和播放状态协调，不写机械重复实现的测试。
- 验证静态产物及仓库子路径，不只运行开发服务器。
- 浏览器验证记录版本、视口和结果；模拟器与真机结果分开说明。
- 未运行、失败或不可用的检查如实记录，不宣称已通过，不用无依据的覆盖率代替行为验证。
- ECC 命令可用时可使用 `/plan`、`/code-review`、`/verify`；不可用时执行等效流程，不擅自安装。
- 提交信息使用中文：`type(scope)` 与类型关键字沿用 Conventional Commits 英文写法，描述和正文用中文。格式、示例和审查要求见 [SDD 维护指南](docs/sdd.md)。

## 7. 完成标准

本次验收项通过、任务状态真实、必要文档已同步且验证结果可追溯，才可标记完成并归档。受阻的必要验收不能用“已知限制”绕过；如需缩减范围，记录用户同意后更新规格。

交付说明包含：修改内容、原因、验证结果、未验证范围及活动或归档记录链接。功能开发请求完成到授权范围内的结果；仅文档任务不自动启动应用实现。
