# 实施计划

对应 [规格](spec.md)。

1. 在 `scripts/` 实现并测试构建期目录扫描：解析目录名，图片自然排序与 `topNN` 优先，合并 `meta.json`，生成 `src/content/generated-photo-index.json`；生成文件不作为手写事实来源。【AC 1-3】
2. 重构 `content` 的模型、校验、构建期资源校验和公开入口：读取生成索引与 `home-memory.json`，移除 `albums.json` 和缩略图契约；保留运行时配置错误降级。【AC 2、4】
3. 改造 `albums` 主回忆纯契约以及两套主题调用点，使其接收显式主回忆条目并不再筛选相册照片。【AC 4】
4. 迁移示例图片、创建三份 `meta.json` 与主回忆配置，删除旧照片/缩略图配置和不再需要的生成脚本；将索引生成接入 `dev`、校验和构建命令。【AC 1、2】
5. 更新单元、构建期、端到端测试及 README、需求、架构、模块说明；执行检查并记录真实结果。【AC 5】

回退：恢复 `albums.json` 与旧资源路径、移除生成步骤即可回到当前静态配置；本次不覆盖源照片，只移动仓库内发布示例资源。
