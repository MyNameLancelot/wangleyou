# 紧凑主题切换计划

1. 先收紧 Playwright 验收，覆盖图标紧贴的圆形表面、44px 命中区、`lucide-react` 循环图标、142px 展开和统计文案缺失。
2. 安装 `lucide-react` 并在海边与草原目录分别替换手写图标。
3. 分别调整两主题开关样式：玻璃库内容层需同时收紧宽度，避免收起时文字把图标挤出圆外。
4. 从两套主题首页删除统计 JSX、参数和样式。
5. 同步需求与模块基线，完成全量验证、截图核对和归档。

回退方式：恢复两主题手写图标与旧尺寸，回退 `lucide-react` 依赖，并恢复两份首页统计 JSX、参数和样式。
