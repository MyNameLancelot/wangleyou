# 任务清单

## 阶段 1：主题运行时

- [x] 1.1 重写 `src/themes/tokens.css`：三套模式，变量命名对齐 Penpot 语义 Token。
- [x] 1.2 实现主题读取、切换与偏好持久化，导出 `applyTheme`/`readTheme`/`nextTheme`/`initTheme`。
- [x] 1.3 更新 `src/themes/module.md`（README 的主题说明在收尾阶段统一更新）。
- [x] 1.4 单测覆盖：默认回退、非法值、持久化、存储异常与循环切换（`src/themes/theme.test.ts`）。

## 阶段 2：内容与播放

- [x] 2.1 `playback` 升级为媒体队列：当前媒体、意图、实际状态、连续播放、进度。
- [x] 2.2 `openSession`/`stepSession` 支持照片与视频，`ended` 按意图推进。
- [x] 2.4 单测覆盖：队列推进、边界、连续播放、意图与实际状态分离（`src/playback/session.test.ts`，10 条）。
- [x] 2.3 内容校验支持视频时长与字幕说明字段；演示内容补一条视频并在 SOURCES.md 标注来源（CC0）。

## 阶段 3：页面结构

- [x] 3.1 首页：影像入口、精选回忆、相册入口、继续浏览/继续播放、主题切换。
- [x] 3.2 影像浏览页：年份分组与定位、图片视频混合网格、类型标识。
- [x] 3.3 相册详情：元信息、视频标识、返回路径、相邻相册导航。
- [x] 3.4 路由新增 `#/browse`、`#/albums` 与未找到状态保持一致。

## 阶段 4：查看器与控制栏

- [x] 4.1 查看器支持视频元素、海报与加载/错误状态。
- [x] 4.2 控制栏：播放暂停、进度、音量、连续播放、上一项/下一项、全屏、退出。
- [x] 4.3 控制栏自动隐藏与恢复，聚焦时永不隐藏。
- [x] 4.4 键盘与触控操作、横竖屏适配、reduced-motion。

## 阶段 5：状态与无障碍

- [x] 5.1 骨架、空状态、错误状态、配置异常与离线提示。
- [x] 5.2 触控目标 44×44、焦点可见、非颜色状态、替代文本与字幕说明。
- [x] 5.3 对比度与 safe-area 检查，1440/390 视口无横向溢出。

## 阶段 6：装饰资产

- [x] 6.1 两套主题装饰层接入（纯 CSS 图形，`aria-hidden` + `pointer-events: none`），可隐藏、不遮挡内容与控制。
- [x] 6.2 运行时实现方式与位图资产的取舍同步到 `docs/design-assets/README.md`。

## 收尾

- [x] 7.1 运行 `npm run check`、`npm run build`、`npm run test:e2e`、`npm run verify:fixtures`。
- [x] 7.2 同步 requirements、architecture、module.md、README。
- [x] 7.3 写 `verification.md`，逐条记录 B1–B12 证据与未验证项。
- [x] 7.4 归档变更目录并更新索引。

## 阶段 8：设计稿视觉对齐（2026-09-17 复核后）

- [x] 8.1 默认主题改为海边沙滩，切换顺序为海边 → 草原 → 默认。
- [x] 8.2 首页改为整幅主题插画 + 左侧内容卡，文案作为主题语气 Slot。
- [x] 8.3 影像卡片改为说明条内嵌底部，首页/浏览 16:7、相册详情 4:3。
- [x] 8.4 影像浏览页新增 hero 带、悬浮筛选胶囊与左侧"按时间定位"卡。
- [x] 8.5 相册详情改为右对齐播放入口与"接下来的影像 + 下一本相册"预告。
- [x] 8.6 页眉标识、主题切换胶囊、页脚脚注对齐设计稿。
- [x] 8.7 主题背景插画导出为 WebP 并登记来源。
