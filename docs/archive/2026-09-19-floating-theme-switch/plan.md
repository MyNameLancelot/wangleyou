# 悬浮主题切换实施计划

1. 在海边与草原主题目录分别新增独立主题切换组件及样式，复用既有轻量液态玻璃依赖。（验收 2–4）
2. 从两套主题应用壳层移除全局顶栏并接入各自悬浮控件。（验收 1–3）
3. 扩展浏览器测试，覆盖无全局导航、位置、展开、键盘焦点和切换行为。（验收 1、3–5）
4. 同步需求、架构和模块基线，运行 check、build、E2E 与 SDD 校验后归档。（验收 1–5）

回退方式：恢复两个主题 App 的原顶栏，并删除各自悬浮控件及对应测试；不影响内容、播放与路由数据。
