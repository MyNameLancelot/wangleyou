# 首页两屏主回忆验证记录

日期：2026-09-19  
状态：完成。

## 自动化证据

- `npm run check`：通过；10 个 Vitest 文件、100 项测试全部通过。
- `npm run build`：通过；内容校验为 3 个相册、7 个媒体，Vite 静态构建成功。
- `npm run test:e2e`：37 项通过、3 项按设备条件跳过、0 项失败。跳过项仅为 desktop 触控和 mobile 原生全屏。

## AC-1 至 AC-7

| 验收 | 结果 | 证据 |
| --- | --- | --- |
| AC-1 | 通过 | 1440×900 下 ArrowDown/ArrowUp 只在 hero 与 memory 两段间切换；两个 section 均为完整视口高度。 |
| AC-2 | 通过 | 纯函数测试覆盖 56px 累积阈值和切屏锁；浏览器覆盖 reduced-motion 与两屏边界。 |
| AC-3 | 通过 | 1920×1080、2560×1440 验证海滩 2K 背景、两屏高度、无横向溢出及液态玻璃 SVG；Safari UA 路径保持标题和操作可读。 |
| AC-4 | 通过 | 浏览器实际点击上一张、下一张、暂停并核对序号和继续播放按钮；相册入口及查看器流程通过。 |
| AC-5 | 通过 | `home-memory.test.ts` 覆盖循环、合格状态、失效与 dispose 清理；查看器打开和路由卸载流程通过。 |
| AC-6 | 通过 | 390×844 验证两主题两屏高度、无横向溢出；移动项目验证触屏切换和 44×44 交互目标。 |
| AC-7 | 通过 | 完整单元、构建与桌面/移动浏览器套件通过。 |

Safari UA 回退测试运行在 Chromium 中，不等同于 Safari/WebKit 真机测试；该限制不影响已验证的渐进增强与可读回退结构。
