# 任务清单

## 阶段 1：主题运行时

- [x] 1.1 重写 `src/themes/tokens.css`：三套模式，变量命名对齐 Penpot 语义 Token。
- [x] 1.2 实现主题读取、切换与偏好持久化，导出 `applyTheme`/`readTheme`/`nextTheme`/`initTheme`。
- [x] 1.3 更新 `src/themes/module.md`（README 的主题说明在收尾阶段统一更新）。
- [x] 1.4 单测覆盖：默认回退、非法值、持久化、存储异常与循环切换（`src/themes/theme.test.ts`）。

## 阶段 2：内容与播放

- [ ] 2.1 `playback` 升级为媒体队列：当前媒体、意图、实际状态、连续播放、进度。
- [ ] 2.2 `openSession`/`stepSession` 支持照片与视频，`ended` 按意图推进。
- [ ] 2.3 内容校验支持视频时长与字幕说明字段；演示内容补一条视频。
- [ ] 2.4 单测覆盖：队列推进、边界、连续播放、意图与实际状态分离。

## 阶段 3：页面结构

- [ ] 3.1 首页：影像入口、精选回忆、相册入口、继续浏览/继续播放、主题切换。
- [ ] 3.2 影像浏览页：年份分组与定位、图片视频混合网格、类型标识。
- [ ] 3.3 相册详情：元信息、视频标识、返回路径、相邻相册导航。
- [ ] 3.4 路由新增 `#/browse` 与未找到状态保持一致。

## 阶段 4：查看器与控制栏

- [ ] 4.1 查看器支持视频元素、海报与加载/错误状态。
- [ ] 4.2 控制栏：播放暂停、进度、音量、连续播放、上一项/下一项、全屏、退出。
- [ ] 4.3 控制栏自动隐藏与恢复，聚焦时永不隐藏。
- [ ] 4.4 键盘与触控操作、横竖屏适配、reduced-motion。

## 阶段 5：状态与无障碍

- [ ] 5.1 骨架、空状态、错误状态、配置异常与离线提示。
- [ ] 5.2 触控目标 44×44、焦点可见、非颜色状态、替代文本与字幕说明。
- [ ] 5.3 对比度与 safe-area 检查，1440/390 视口无横向溢出。

## 阶段 6：装饰资产

- [ ] 6.1 两套主题装饰层接入，可隐藏可裁切，不遮挡内容与控制。
- [ ] 6.2 资源导出与命名说明同步到 `docs/design-assets/README.md`。

## 收尾

- [ ] 7.1 运行 `npm run check`、`npm run build`、`npm run test:e2e`。
- [ ] 7.2 同步 requirements、architecture、module.md、README。
- [ ] 7.3 写 `verification.md`，逐条记录 B1–B12 证据与未验证项。
- [ ] 7.4 归档变更目录并更新索引。
