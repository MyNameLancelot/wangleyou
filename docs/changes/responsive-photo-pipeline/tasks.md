# 响应式相册媒体管线任务

- [ ] 建立 full 变更记录并确认规格、计划和验收项。
- [ ] 为增量 WebP 派生器编写失败测试：元数据、哈希命中、单图失效、过期清理和原图不变。
- [ ] 实现派生器、清单与 CLI，生成带哈希路径的 480/960/1600/2560 WebP。
- [ ] 迁移照片源目录，更新索引生成与内容/发布资源校验。
- [ ] 调整 npm 命令、`.gitignore`、GitHub Actions 媒体缓存与缓存失效策略。
- [ ] 安装 `react-photo-album`，在两个主题独立接入 Masonry 与响应式 `sizes`。
- [ ] 相册详情改为 1:1 采用 `react-photo-album` 默认档位（列数 5/4/3/2、间距 20/15/10/5、组件自动 `sizes`），并同步需求与端到端断言。
- [ ] 为相册详情加入白色相纸相框（`padding={8}` + 白底、8px 圆角、主题色轻投影），确认不改变列数档位与无描边焦点指示，并补端到端断言。
- [ ] 全局清除指针点击后的浏览器默认聚焦框（`:focus:not(:focus-visible)` 覆盖按钮、链接、表单控件、`summary`、`video`、`dialog`），保留键盘焦点环，并补端到端断言。
- [ ] 全站按钮在任何状态都不使用边框：补全局零边框基线，移除主题中按钮的实色与透明占位边框并用底色替代，补跨页面五状态断言。
- [ ] 媒体目录去掉 `photos` 中间层（`media-source/<相册目录>/` 与 `public/media/<相册目录>/`），同步脚本、CI 缓存键、忽略规则、测试与文档，并把派生布局纳入缓存版本。
- [ ] 次要按钮 `.secondary` 在悬浮、聚焦、按下时保持静态外观，并补断言。
- [ ] 更新内容、脚本和 E2E 测试，覆盖三视口、两主题、查看器打开与资源尺寸。
- [ ] 更新 requirements、architecture、module.md、README、ADR 与 change.json。
- [ ] 执行 `npm run check`、`npm run build`、`npm run test:e2e` 和静态产物核对，记录真实结果。
