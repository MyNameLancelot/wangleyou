# 任务

- [x] P1 新增并测试 `mediaUrl()`，保持 `assetUrl()` 兼容（AC-1、AC-2、AC-4）。
- [x] P2 迁移两个主题的全部媒体消费点并静态核对统一入口（AC-3）。
- [ ] P3 运行默认/CDN 构建、检查及浏览器回归，记录结果（AC-1、AC-2、AC-5）。默认/CDN 构建和 `npm run check` 已通过；E2E 被用户已有未提交的海边背景测试改动阻塞，详见 [verification.md](verification.md)。
- [ ] P4 已同步需求、架构、模块、README 与验证记录；待 CDN 外网环境及用户已有海边背景 E2E 断言协调后归档（AC-5）。
