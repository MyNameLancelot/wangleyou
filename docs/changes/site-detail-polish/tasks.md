# 网站整体细节修复任务

本任务清单持续追加；完成状态只反映实际执行结果。

## SD-001 全站默认禁止选择展示文字与图片

- [x] 判定为 full 并记录已确认的范围、例外与验收条件
- [x] 在 app 全局样式禁止展示文字选择、图片选择与原生拖拽
- [x] 为可编辑字段保留文字选择
- [x] 同步 requirements 与 app module 基线
- [x] 运行检查、构建与浏览器行为验证并记录真实结果

## SD-002 留影页返回首页入口

- [x] 判定为 full，记录用户确认的 hero 内文字链接方案与验收条件
- [x] 在海边主题留影页增加返回首页链接与私有样式
- [x] 在草原主题留影页增加返回首页链接与私有样式
- [x] 增加两个主题的路由、尺寸与窄视口 E2E 断言
- [x] 同步 requirements 与 themes module 基线
- [x] 运行检查、构建与浏览器验证并记录真实结果

## SD-005 移动端留影页顶部固定

- [x] 判定为 full 并记录固定范围
- [x] 在两个主题固定 hero 与年份导航并为列表预留空间
- [x] 验证移动端滚动、操作与无溢出
- [x] 拆分固定透明标签与年份按钮横向滚动区，避免覆盖
- [x] 为手机固定年份导航容器恢复不透明主题画布底色，遮住滚动列表
- [x] 调整固定层级，使筛选胶囊完整显示在年份遮罩之上
- [x] 为手机年份定位补足固定操作区滚动偏移，避免目标标题被遮挡

## SD-007 扩展假相册数据用于滑动验证

- [x] 新增 2026–2029 四册假数据及发布图片
- [x] 生成索引并验证内容与移动端浏览

## SD-008 手机主题切换按钮固定

- [x] 判定为 full 并记录手机固定、PC/平板跟随滚动的边界
- [x] 在海边、草原主题私有 App CSS 中实现 ≤600px 固定定位
- [x] 验证手机固定与 >600px 跟随滚动
- [x] 将手机固定范围收窄为 `#/browse`，验证相册详情随滚动

## SD-009 首页主回忆手动换图重置自动轮播

- [x] 判定为 full 并记录手动、自动与暂停的计时边界
- [x] 为 albums interval 控制器增加仅运行中重启周期的操作
- [x] 在两个主题统一所有手动换图入口
- [x] 验证计时器重置、暂停不重启及构建
- [x] 在 768×1024 触控平板视口验证第二屏横滑换图

## SD-010 手机与平板查看器隐藏切换按钮

- [x] 在两套主题 ≤900px 查看器样式隐藏上一项、下一项按钮
- [x] 验证手机与平板宽度的横滑切换仍可用
- [x] 让手机与平板查看器照片使用全部可用横向宽度，保持原比例
- [x] 修正查看器范围为 ≤1199px（含 1024px iPad），并确认手机窄屏规则不再回写横向内边距

## SD-011 照片查看模式移除关闭按钮

- [x] 判定为 full 并记录三端移除范围与保留的退出入口
- [x] 两个主题按媒体类型收窄关闭按钮渲染条件
- [x] 为无控件照片模式补充 Tab 焦点兜底
- [x] 运行构建与完整 Playwright 套件并记录真实结果

## SD-012 手机与平板相册详情压缩首幅并缩小图集

- [x] 判定为 full 并记录三分之二首幅、图集列数与首屏张数边界
- [x] 在 ≤600px 与平板断点收紧首幅高度与横幅字号
- [x] 手机图集改双列、平板与 1024px iPad 改三列
- [x] 运行构建与完整 Playwright 套件并记录真实结果
- [x] 判定为 full 的 SD-012 更正：横幅再降 1/5、手机改三列、缩略图容器贴合图片
- [x] 运行构建与完整 Playwright 套件并记录 SD-012 与更正后的真实结果

## SD-013 手机端隐藏返回入口并重新居中 hero 文案

- [x] 判定为 full 并记录手机隐藏、平板/桌面保留的边界
- [x] 两个主题 ≤600px 隐藏“返回首页”与“返回留影”并调整 hero 文案偏移
- [x] 更新受影响用例并为四视口新增可见性与边界断言
- [x] 运行构建与完整 Playwright 套件并记录真实结果

## SD-014 手机端留影页相册卡片只渲染相册名

- [x] 判定为 full 并记录手机只显示相册名、底色框贴合的边界
- [x] 两个主题 ≤600px 隐藏相册说明并让底色框贴合文字
- [x] 新增手机与平板/桌面差异断言
- [x] 运行构建与完整 Playwright 套件并记录真实结果

## SD-004 缩略图卡片移除红色外焦点框

- [x] 判定为 light，记录保留键盘焦点反馈的视觉修复范围
- [x] 在海边与草原主题移除缩略图焦点描边
- [x] 增加两个主题的无描边与查看器入口 E2E 断言
- [x] 运行检查、构建与浏览器验证并记录真实结果

### SD-003 更正：背景点击关闭

- [x] 记录用户对背景点击关闭的明确更正
- [x] 在海边、草原查看器添加背景点击关闭分流
- [x] 更新 E2E 为背景关闭、图片与控件不关闭
- [x] 同步基线并重新验证、记录结果

## SD-003 查看器仅允许显式关闭

- [x] 判定为 full，记录用户确认的唯一关闭入口与验收条件
- [x] 在海边查看器阻止原生取消路径关闭
- [x] 在草原查看器阻止原生取消路径关闭
- [x] 增加两个主题的空白点击、导航与 Esc E2E 断言
- [x] 同步 requirements 与 themes module 基线
- [x] 运行检查、构建与浏览器验证并记录真实结果
