# 主题设计资产

本目录保存 Penpot 设计基线使用的原创演示资产，不是当前前端运行时资源。未经后续实现变更，不应从这里直接建立产品代码依赖。

| 文件 | 主题 | 角色 | 桌面行为 | 移动行为 | 导出建议 |
| --- | --- | --- | --- | --- | --- |
| `beach-environment-v1.png` | 海边沙滩 | `decorative` 背景层 | 宽度填满，中央低细节区承载标题或内容；椰树、贝壳、漂流木和礁石保持在边缘 | 以海浪与沙滩为中心裁切；允许隐藏椰树和大部分贝壳，不影响功能 | 实现时优先重新导出 WebP；如需分层动效，将潮线与边缘装饰另制 SVG |
| `grassland-environment-v1.png` | 旷野草原 | `decorative` 背景层 | 宽度填满，以地平线组织分区；树、野花与小路保持在边缘和下沿 | 保留天空、山脊与少量草地；隐藏边缘树木和密集野花 | 实现时优先重新导出 WebP；若需要低成本层移，只拆分远山和前景草地 |
| `beach-keepsake-v1.png` | 海边沙滩 | `decorative` 边缘点缀 | 贝壳、卵石和漂流木只放在非媒体内容边缘 | 可全部隐藏 | 单独裁成透明 WebP 或 SVG；不得合入交互图标 |
| `grassland-keepsake-v1.png` | 旷野草原 | `decorative` 边缘点缀 | 野花和草叶只放在非媒体内容边缘 | 可全部隐藏 | 单独裁成透明 WebP 或 SVG；不得合入交互图标 |

`build-theme-screens.mjs` 生成 `*-screens-v2.svg/png`、`*-mobile-v2.svg/png` 及按 Home/Browse/Album/Viewer 拆分的 PNG。它们是 Penpot 高保真演示素材和设计回溯源，不是应当整图塞进 React 页面或承担真实业务数据的资产。前端实现须重建语义结构、组件与设计变量；图中“演示媒体占位”不应被写死为相册内容。

## 共同约束

- 图片和视频始终是视觉主体；环境插画不得叠盖媒体、播放控制、字幕或错误恢复操作。
- 资产可裁切、可隐藏，不承载业务语义；无装饰时界面仍应完整可用。
- 1440 参考画板允许完整环境层；390 参考画板只保留能辨认主题的局部。
- `prefers-reduced-motion` 下不移动背景层，不使用视差，只允许 120ms 以内淡入或即时切换。
- 本次图像由内置 image generation 工具生成，无真实家庭影像、品牌标识、文字或水印。
